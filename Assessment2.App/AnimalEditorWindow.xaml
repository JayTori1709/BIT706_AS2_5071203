<Window x:Class="Assessment2.App.AnimalEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="We-Treat-It: Add Animal" Height="250" Width="400" ResizeMode="NoResize">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.Resources>
            <Style TargetType="ComboBox">
                <Setter Property="Width" Value="250" />
                <Setter Property="Margin" Value="4" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Right" />
                <Setter Property="Margin" Value="4" />
            </Style>
            <Style TargetType="TextBox">
                <Setter Property="Width" Value="250" />
                <Setter Property="Margin" Value="4" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Type:" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Sex:" />
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Breed:" />
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Owner:" />

        <TextBox x:Name="txtName" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" />
        <TextBox x:Name="txtType" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" />
        <ComboBox x:Name="cmbSex" Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2">
            <ComboBoxItem Content="Unknown" />
            <ComboBoxItem Content="Female" />
            <ComboBoxItem Content="Male" />
        </ComboBox>
        <TextBox x:Name="txtBreed" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" />
        <TextBox x:Name="owner" Grid.Row="4" Grid.Column="1" Width="220" IsReadOnly="True" />
        <Button Grid.Row="4" Grid.Column="2" Height="20" Width="20" Margin="2,4" Click="OnFindCustomer">
            <Image Source="Images/search.png" Height="16" Width="16" />
        </Button>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="50" />
                    <Setter Property="Margin" Value="4" />
                </Style>
            </StackPanel.Resources>
            <Button Content="Cancel" IsCancel="True" Click="Cancel_Click" />
            <Button Content="Save" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
