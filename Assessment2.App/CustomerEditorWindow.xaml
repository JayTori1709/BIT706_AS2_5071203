<Window x:Class="Assessment2.App.BusinessLayer.CustomerEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Editor"
        Height="350"
        Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="First Name:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding FirstName}" Grid.Row="0" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Surname:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding Surname}" Grid.Row="1" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Phone Number:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
            <TextBox Text="{Binding PhoneNumber}" Grid.Row="2" Grid.Column="1" Margin="5"/>

            <TextBlock Text="Address:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="5"/>
            <TextBox Text="{Binding Address}" Grid.Row="3" Grid.Column="1" Height="100" Margin="5" AcceptsReturn="True" TextWrapping="Wrap"/>
        </Grid>

        <StackPanel Orientation="Horizontal"
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Margin="5">
            <Button Content="Cancel"
                    Width="80"
                    Margin="5,0"
                    Click="OnCancel"/>
            <Button Content="Save"
                    Width="80"
                    Margin="5,0"
                    Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
