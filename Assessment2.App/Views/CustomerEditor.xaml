<Window x:Class="Assessment2.App.Views.CustomerEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Assessment2.App.ViewModels"
        mc:Ignorable="d"
        Title="Customer Editor" Height="450" Width="700"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:CustomerViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0"
                  ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  Margin="0 0 0 10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="Surname" Binding="{Binding Surname}" Width="*" />
                <DataGridTextColumn Header="Phone Number" Binding="{Binding PhoneNumber}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add" Command="{Binding AddCustomerCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Edit" Command="{Binding EditCustomerCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Delete" Command="{Binding DeleteCustomerCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
